import { color } from './color';
import { teacherColor } from './color';
import { font } from './font';

export const theme = {
  color: color,
  teacherColor: teacherColor,
  font: font,
} as const;
